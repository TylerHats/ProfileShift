<Window x:Class="ProfileShift.UI.Views.LogViewerModal"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Log Viewer - ProfileShift" Height="580" Width="750"
        Background="#2F3136" Foreground="#D9D9D9"
        WindowStartupLocation="CenterOwner" ResizeMode="CanResize"
        FontFamily="Segoe UI" FontSize="13">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../Themes/DarkTheme.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <Grid Margin="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Search & Filters -->
        <Grid Grid.Row="0" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox x:Name="TxtSearch" Grid.Column="0" Height="30" Padding="8,4" VerticalContentAlignment="Center" Background="#40444B" Foreground="#D9D9D9" TextChanged="TxtSearch_TextChanged"/>
            <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="12,0,0,0" VerticalAlignment="Center">
                <CheckBox x:Name="ChkInfo" Content="Info" IsChecked="True" Foreground="#43B581" Margin="4,4,10,4" Click="Filter_Click"/>
                <CheckBox x:Name="ChkWarning" Content="Warning" IsChecked="True" Foreground="#E67E22" Margin="4,4,10,4" Click="Filter_Click"/>
                <CheckBox x:Name="ChkError" Content="Error" IsChecked="True" Foreground="#F04747" Margin="4,4,0,4" Click="Filter_Click"/>
            </StackPanel>
        </Grid>

        <!-- Log Table Container -->
        <Border Grid.Row="1" Background="#36393F" CornerRadius="6" BorderBrush="#40444B" BorderThickness="1" Padding="2">
            <DataGrid x:Name="DgLogs" AutoGenerateColumns="False" IsReadOnly="True" Background="Transparent" RowBackground="#36393F" AlternatingRowBackground="#2F3136" Foreground="#D9D9D9" GridLinesVisibility="None" BorderThickness="0" HeadersVisibility="Column">
                <DataGrid.Columns>
                    <DataGridTemplateColumn Header="Timestamp" Width="120">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Timestamp}" Foreground="#99AAB5" FontFamily="Consolas" FontSize="11.5" VerticalAlignment="Center"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTemplateColumn Header="Level" Width="95">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border CornerRadius="3" Padding="6,2" HorizontalAlignment="Left" VerticalAlignment="Center">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Level}" Value="Info">
                                                    <Setter Property="Background" Value="#2243B581"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Level}" Value="Warning">
                                                    <Setter Property="Background" Value="#33E67E22"/>
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Level}" Value="Error">
                                                    <Setter Property="Background" Value="#33F04747"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <TextBlock Text="{Binding Level}" FontWeight="Bold" FontSize="11">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Setter Property="Foreground" Value="#43B581"/>
                                                <Style.Triggers>
                                                    <Trigger Property="Text" Value="Warning">
                                                        <Setter Property="Foreground" Value="#E67E22"/>
                                                    </Trigger>
                                                    <Trigger Property="Text" Value="Error">
                                                        <Setter Property="Foreground" Value="#F04747"/>
                                                    </Trigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTemplateColumn Header="Message" Width="*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding Message}" Foreground="#D9D9D9" TextWrapping="Wrap" VerticalAlignment="Center" Margin="2,0"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Border>

        <!-- Dialog Buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,14,0,0">
            <Button x:Name="BtnExportTxt" Content="Export to TXT" Width="120" Height="34" Background="#40444B" Foreground="White" Click="BtnExportTxt_Click" Margin="0,0,8,0"/>
            <Button x:Name="BtnClose" Content="Close" Width="95" Height="34" Background="#7289DA" Foreground="White" FontWeight="Bold" Click="BtnClose_Click"/>
        </StackPanel>
    </Grid>
</Window>
